<h1>Tasks</h1>
<%= render 'form' %>
<ul>
  <% @tasks.each do |task| %>
    <li id="task_<%= task.id %>">
      <%= task.task %>
      | <%= link_to 'Edit' , edit_task_path(task) %>
      | <%= link_to 'Delete' , delete_task_path(task) %>
    </li>
  <% end %>
</ul>

